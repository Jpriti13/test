// Step definitions for product feature

Given('I have a product with name {string}', (name) => {
    // Implementation here
});

When('I add the product to the cart', () => {
    // Implementation here
});

Then('I should see the product in the cart', () => {
    // Implementation here
});
