// Checkout step definitions

Given('I am on the checkout page', function () {
    // code to navigate to checkout page
});

When('I enter the shipping information', function (data) {
    // code to enter shipping information
});

When('I enter the payment information', function (data) {
    // code to enter payment information
});

Then('I should see the order confirmation', function () {
    // code to verify order confirmation
});

Then('I should see an error message', function () {
    // code to verify error message
});
